<Window x:Class="EasySaveClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Console de sauvegarde distante" Height="450" Width="800">
    <DockPanel Margin="10">
        <!-- Boutons en bas -->
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="▶ Play" Width="80" Click="Play_Click" Margin="5,0"/>
            <Button Content="⏸ Pause" Width="80" Click="Pause_Click" Margin="5,0"/>
            <Button Content="⏹ Stop" Width="80" Click="Stop_Click" Margin="5,0"/>
        </StackPanel>


        <!-- DataGrid principal -->
        <DataGrid ItemsSource="{Binding Jobs}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Progression" Binding="{Binding Progress}" Width="100"/>
                <DataGridTextColumn Header="État" Binding="{Binding State}" Width="100"/>
                <DataGridTemplateColumn Header="Progression Visuelle" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ProgressBar Minimum="0" Maximum="100"
                                         Value="{Binding ProgressValue}"
                                         Height="20"
                                         VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
